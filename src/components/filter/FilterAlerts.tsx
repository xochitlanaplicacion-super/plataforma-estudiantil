'use client';

import { FormEvent, useState, useTransition } from 'react';
import { useRouter } from 'next/navigation';
import { BellRing, Save, ShieldCheck } from 'lucide-react';
import { updateFilterAlertSettings } from '@/lib/actions/filter-control';
import { useToast } from '@/hooks/use-toast';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Switch } from '@/components/ui/switch';

export function FilterAlerts({ initialData }: { initialData: any }) {
  const settings = initialData.settings || {}; const router = useRouter(); const { toast } = useToast(); const [pending, startTransition] = useTransition();
  const verifiedGuardianPolicyAvailable = typeof settings.require_verified_guardian_contact === 'boolean';
  const [enabled, setEnabled] = useState(settings.enabled ?? true); const [threshold, setThreshold] = useState(Number(settings.threshold || 3)); const [unit, setUnit] = useState(settings.window_unit || 'global'); const [value, setValue] = useState(Number(settings.window_value || 1));
  const [requireVerifiedGuardianContact, setRequireVerifiedGuardianContact] = useState(settings.require_verified_guardian_contact ?? true);
  const submit = (event: FormEvent) => { event.preventDefault(); startTransition(async () => { const result = await updateFilterAlertSettings({ enabled, threshold, windowUnit: unit, windowValue: value, requireVerifiedGuardianContact }); if (!result.success) toast({ variant: 'destructive', title: 'No se guardó', description: result.error }); else { toast({ title: result.verifiedGuardianPolicyAvailable ? 'Políticas institucionales guardadas' : 'Política de alertas guardada', description: result.warning }); router.refresh(); } }); };
  return <div className="mx-auto max-w-3xl space-y-6"><div><h1 className="flex items-center gap-2 text-3xl font-bold text-primary"><BellRing />Alertas y políticas de seguridad</h1><p className="text-muted-foreground">Reglas institucionales persistentes y aisladas para esta escuela.</p></div><Card><CardHeader><CardTitle>Políticas institucionales</CardTitle><CardDescription>Los cambios se aplican únicamente a este plantel y quedan registrados en auditoría.</CardDescription></CardHeader><CardContent><form onSubmit={submit} className="space-y-6"><section className="space-y-5"><div className="flex items-center justify-between rounded-xl border p-4"><div><p className="font-semibold">Mostrar alerta por reincidencia</p><p className="text-sm text-muted-foreground">Resalta al alumno cuando alcanza el umbral.</p></div><Switch checked={enabled} onCheckedChange={setEnabled} aria-label="Mostrar alerta por reincidencia" /></div><div className="grid gap-4 sm:grid-cols-3"><div><Label>A partir de cuántos retardos</Label><Input type="number" min={1} max={100} value={threshold} onChange={(e) => setThreshold(Number(e.target.value))} /></div><div><Label>Periodo</Label><Select value={unit} onValueChange={setUnit}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="global">Histórico global</SelectItem><SelectItem value="days">Días</SelectItem><SelectItem value="months">Meses</SelectItem><SelectItem value="years">Años</SelectItem></SelectContent></Select></div><div><Label>Cantidad del periodo</Label><Input type="number" min={1} max={100} disabled={unit === 'global'} value={value} onChange={(e) => setValue(Number(e.target.value))} /></div></div><div className="rounded-xl bg-primary/5 p-4 text-sm"><strong>Regla resultante:</strong> alerta al llegar a {threshold} retardo(s) {unit === 'global' ? 'en todo el historial' : `durante los últimos ${value} ${unit === 'days' ? 'días' : unit === 'months' ? 'meses' : 'años'}`}.</div></section>{verifiedGuardianPolicyAvailable && <section className="space-y-3 border-t pt-6"><div className="flex items-start justify-between gap-4 rounded-xl border border-primary/25 bg-primary/5 p-4"><div><p className="flex items-center gap-2 font-semibold"><ShieldCheck className="h-4 w-4 text-primary" />Exigir contacto oficial verificado</p><p className="mt-1 text-sm text-muted-foreground">Controla las entregas extraordinarias. Activado: el trámite se bloquea hasta seleccionar un contacto verificado del alumno. Desactivado: permite documentar manualmente quién autorizó, conservando evidencia y responsables.</p></div><Switch checked={requireVerifiedGuardianContact} onCheckedChange={setRequireVerifiedGuardianContact} aria-label="Exigir contacto oficial verificado en entregas extraordinarias" /></div><p className="text-sm font-medium">Estado: {requireVerifiedGuardianContact ? 'Obligatorio' : 'Opcional con autorización manual documentada'}</p></section>}<Button type="submit" disabled={pending}><Save className="mr-2 h-4 w-4" />{pending ? 'Guardando…' : verifiedGuardianPolicyAvailable ? 'Guardar políticas' : 'Guardar política'}</Button></form></CardContent></Card></div>;
}
